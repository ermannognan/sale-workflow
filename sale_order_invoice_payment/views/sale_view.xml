<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="view_order_tree" model="ir.ui.view">
            <field name="name">sale.order.tree</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_tree"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='invoice_status']" position="before">
                    <field name="payment_status"/>
                </xpath>

            </field>
        </record>

        <record id="view_order_form" model="ir.ui.view">
            <field name="name">sale.order.form</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">

                <xpath expr="//group[@name='sale_pay']/field[@name='invoice_status']" position="after">
                    <field name="payment_status"/>
                </xpath>

            </field>
        </record>

        <record id="view_sales_order_filter" model="ir.ui.view">
            <field name="name">sale.order.list.select</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_sales_order_filter"/>
            <field name="arch" type="xml">

                <field name="user_id" position="after">
                    <field name="payment_status"/>
                </field>

                <filter name="my_sale_orders_filter" position="before">
                    <filter string="Not to pay" domain="[('payment_status', '=', 'no')]" name="sale_orders_not_to_pay"/>
                    <filter string="Waiting for payment" domain="[('payment_status', '=', 'waiting')]" name="sale_orders_waiting_for_payment"/>
                    <filter string="Paid" domain="[('payment_status', '=', 'done')]" name="sale_orders_paid"/>
                    <separator/>
                </filter>

            </field>
        </record>

</odoo>
